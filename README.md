# pi-sub

A clean wrapper around `pi --mode json` for calling Pi non-interactively from other agents/scripts.

- Streams denoised progress to stderr.
- Emits a clean result/transcript to stdout.
- Supports `pi-sub show` for local session inspection.
